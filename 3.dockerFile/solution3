Напишите Dockerfile для Python приложения соблюдая Best Practicies

Решение:
# Устанавливаем образ Python и устанавливаем его в наш образ Docker. 
# Alpine — это легкий дистрибутив Linux, который будет служить ОС, на которую мы устанавливаем наш образ

FROM python:3.11-alpine

# Копируем requirements файл, предварительно его создав, и его содержимое (сгенерированные пакеты и зависимости) 
# в app папку образа.

COPY ./requirements.txt /app/requirements.txt

# Устанавливаем рабочий каталог как app, который будет корневым каталогом приложения в контейнере.

WORKDIR /app

# Устанавливаем все зависимости, из файла requirements.txt в наше приложение внутри контейнера.

RUN pip install -r requirements.txt

# Копируем все остальные файлы и их содержимое в app папку, 
# которая является корневым каталогом приложения в контейнере.

COPY . /app

# Команда, которая запускает приложение в контейнере.

ENTRYPOINT ["python"]

# Добавляеv список параметров к EntryPoint для выполнения команды, запускающей приложение. 

CMD ["app.py"]
